<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper PUBLIC
"-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="projectdetailMapper"><!--comment-->

	<resultMap id="HotListMap" type="Likes">
		<result property="project_name" column="project_name"/>
		<result property="likecount" column="likecount"/>
	</resultMap>
	
	<select id="selectHotList" resultMap="HotListMap">
	<![CDATA[
		SELECT PROJECT_NAME,COUNT(L.MEMBER_ID) as likecount FROM LIKES L
	    JOIN PROJECT P ON(P.PROJECT_ID=L.PROJECT_ID)
		GROUP BY PROJECT_NAME
		ORDER BY 2 DESC
	]]>
	</select>
		<select id="selectHotList1" resultMap="HotListMap">
	<![CDATA[
		SELECT PROJECT_NAME,COUNT(L.MEMBER_ID) as likecount FROM LIKES L
	    JOIN PROJECT P ON(P.PROJECT_ID=L.PROJECT_ID)
		GROUP BY PROJECT_NAME
		ORDER BY 2 DESC
	]]>
	</select>
	
	
	
	
	
	
</mapper>
